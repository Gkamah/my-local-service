<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <!-- Load Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Load Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <!-- Load Global Custom Styles -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- Specific utility styles for the application -->
    <style>
        /* Classes that rely on CSS variables defined in style.css */
        .profile-photo-lg { 
            width: 150px; 
            height: 150px; 
            object-fit: cover; 
            border: 4px solid white; 
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15); 
        }
        .provider-card {
            transition: all 0.25s ease-in-out;
            cursor: pointer;
            border-left: 4px solid transparent;
        }
        .provider-card:hover {
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
            transform: translateY(-4px);
            border-left-color: var(--color-primary);
        }
        .provider-card.active {
            border-left-color: var(--color-primary);
            background-color: #eef2ff; /* Very light indigo */
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
<!-- The <body> tag is opened here but will be closed in the template file -->
